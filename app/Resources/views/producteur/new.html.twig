{% extends 'base.html.twig' %}

{% block body %}
        {{ include(':producteur:baseProducteur.html.twig') }}
    <ul>
        <li>
            <a href="{{ path('producteur_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
